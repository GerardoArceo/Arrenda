import { TextMaskDirective } from './textmask.directive';

describe('TextMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new TextMaskDirective();
    expect(directive).toBeTruthy();
  });
});
